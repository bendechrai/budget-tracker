[build]
builder = "RAILPACK"

[deploy]
startCommand = "HOSTNAME=0.0.0.0 npx next start --port ${PORT:-3000}"
preDeployCommand = "npx prisma migrate deploy"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
