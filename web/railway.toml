[build]
builder = "RAILPACK"

[deploy]
startCommand = "npx next start -H 0.0.0.0 --port ${PORT:-3000}"
preDeployCommand = "npx prisma migrate deploy"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
